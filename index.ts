// Export the main Tracker class
export { default as Tracker } from './src/tracker';

// Export additional API modules
export { Contacts } from './src/api/contacts'; // Only if you expect users to access Contacts directly
